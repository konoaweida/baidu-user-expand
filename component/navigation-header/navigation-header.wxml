<!-- 顶部占位：高度 = 胶囊顶部距离 + 胶囊高度 + 额外12rpx（rpx 单位直接使用） -->
<view class="top-placeholder" style="height: {{menuTop + menuHeight + 12}}rpx;"></view>

<!-- 固定头部容器：总高度与占位高度一致 -->
<view class="navigation" style="top: 0; height: {{menuTop + menuHeight + 12}}rpx;">
  <!-- 导航核心区域：基于胶囊位置定位（使用 rpx 单位适配） -->
  <view 
    class="navigation-header" 
    style="top: {{menuTop - 12}}rpx; height: {{menuHeight + 24}}rpx; padding-top: 12rpx; padding-bottom: 12rpx;"
  >
    <!-- 插槽：接收外部传入的内容（如标题、图标等） -->
    <slot></slot>
  </view>
</view>